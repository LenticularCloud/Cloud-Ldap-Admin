{% extends "CloudFrontBundle::base.html.twig" %}

{% block content %}
    <h1 class="text-center">Set new Password</h1>
    <div class="col-md-4 col-sm-3">

    </div>
    <div class="col-md-4 col-sm-6">
        {% if user %}
            <h3>for user: {{ user.username }}</h3>
            {{ form(form_password_reset_do) }}
        {% else %}
            token not correct
        {% endif %}
    </div>

{% endblock %}
