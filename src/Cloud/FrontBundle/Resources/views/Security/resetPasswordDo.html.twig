{% extends "CloudFrontBundle::base.html.twig" %}

{% block content %}
    <div class="col-md-4 col-sm-3">

    </div>
    <div class="col-md-4 col-sm-6">
        {% if valid %}
        {{ form(form_password_reset_do) }}
        {% else %}
            token not correct
        {% endif %}
    </div>

{% endblock %}
